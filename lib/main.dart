
import 'package:flutter/material.dart';
import 'package:sqllitte/usertable.dart';

void main() {
  runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      home: user()));
  // home: ChatScreen()));
  // home :Reset()));
}
